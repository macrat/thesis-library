build:
  languages:
    - node

  pre:
    - GOOGLE_APPLICATION_CREDENTIALS=`pwd`/google-cert.json
    - printenv GCLOUD_SERVICE_ACCOUNT > ${GOOGLE_APPLICATION_CREDENTIALS}

  post:
    - npm test

run:
  web: npm start
